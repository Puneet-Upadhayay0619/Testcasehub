{
  "name": "testcasehub-automation",
  "private": true,
  "version": "1.0.0",
  "description": "CI automation runner + Playwright reporter for TestCaseHub.",
  "scripts": {
    "setup-once": "node setup-once.js",
    "test": "node run-automation.js",
    "playwright": "npx playwright test",
    "declarative-checks": "node run-declarative-checks.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.47.0"
  },
  "engines": {
    "node": ">=18"
  }
}
